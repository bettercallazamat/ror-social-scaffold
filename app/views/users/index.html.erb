<section class="users-section">
    <ul class="users-list">
        <% @users.each do |user| %>
        <li>
            Name: <%= link_to user.name,  user_path(user) %>
            <%= friendship_status(user) %>
        </li>
        <% end %>
    </ul>
</section>